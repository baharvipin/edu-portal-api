# edu-portal-api

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/schooldb?schema=public"
postgres → username
postgres → password
localhost → host
5432 → default port
schooldb → database name
